import React from 'react'

export default class Sort extends React.Component {

    render() {
        return(
            <div className="sort">
                {/* <button className="btn btn-primary" onClick={() => this.sortBy()}>Sort by Name</button> */}
                <button className="btn btn-primary">Sort by Name</button>

            </div>
        )
    }

}